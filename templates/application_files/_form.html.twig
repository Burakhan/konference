{{ form_start(form) }}
<div class="card card-lg">
    <div class="card-body">
        <div class="col-md-12">
            <div class="mb-3">
                Dosya Ekle
                <div class="form-file">
                    {{ form_widget(form.file, {'attr': {'class': 'form-file-input'}}) }}

                </div>
            </div>
        </div>
    </div>
    <div class="card-footer text-right">
        <div class="d-flex">
            <button type="submit" class="btn btn-primary ml-auto">{{ button_label|default('Kaydet ve Devam Et') }}</button>
        </div>
    </div>
</div>
{{ form_end(form) }}
