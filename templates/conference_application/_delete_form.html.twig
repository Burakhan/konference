<form method="post" action="{{ path('conference_application_delete', {'id': conference_application.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ conference_application.id) }}">
    <button class="btn">Delete</button>
</form>
